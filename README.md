# Todos REST API application

## Install

    composer install

## Enviroment

    Rename or copy `.env.example` file to `.env`
    php artisan key:generate

    Set your `APP_URL` in your `.env` file.
    php artisan migrate --seed
## Run the app

    php artisan serve
